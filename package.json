{
  "name": "radar-fidc-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke tests for the Radar FIDC static dashboard.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium --with-deps",
    "serve": "python3 -m http.server 8000 --bind 127.0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "1.45.0",
    "typescript": "5.4.5",
    "@types/node": "20.12.12"
  },
  "engines": {
    "node": ">=20"
  }
}
